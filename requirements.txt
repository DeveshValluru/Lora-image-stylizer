--extra-index-url https://download.pytorch.org/whl/cu128

# --- CUDA-pinned wheels (PyTorch index) ---
# torch 2.7+ required by recent transformers (uses torch.float8_e8m0fnu).
# cu128 channel is the right one for torch 2.7+ — cu124 only goes up to 2.6.
torch>=2.7
torchvision>=0.22

# --- Diffusion + LoRA training stack ---
diffusers[training]>=0.30
transformers>=4.46
accelerate>=1.1
peft>=0.13
datasets>=3.0
bitsandbytes>=0.44
safetensors

# --- Inference server ---
fastapi
uvicorn[standard]
gradio

# --- Utilities ---
pyyaml
pillow
tqdm
